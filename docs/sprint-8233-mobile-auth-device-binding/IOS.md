# IOS

Código JS/CSP usa `capacitor://localhost` e está pronto para Keychain via `SecureStorage` plugin.

Build iOS continua dependente de macOS + Xcode. Pasta `ios/` **não** foi gerada nesta sprint.

Universal Links (`redefinir-senha`) ficam para slice posterior.
