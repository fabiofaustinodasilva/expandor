# CHANGELOG

## Sprint 8.2.25

- `users.session_version` (migration aditiva)
- `SellerSingleSessionService` + middleware `seller.single-session`
- Seller: último login vence; Admin/Manager multi-device
- Password reset invalida sessões web via version bump
- Map JS `mapFetch` trata 401/419 → login
- Docs + `Sprint8225SingleDeviceSessionTest`
