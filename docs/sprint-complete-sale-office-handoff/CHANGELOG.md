# Changelog

- Captura completa na venda web (cliente, endereço, vencimento 5–30, produtos do catálogo).
- Persistência em transaction existente do `VisitService`.
- Handoff regenerável (`SaleHandoffData` + formatter).
- Config WhatsApp do escritório por empresa.
- Sem ERP, sem APK, sem offline.
