# ACCESSIBILITY

Correções óbvias — sem certificação formal.

| Tema | Estado |
|------|--------|
| `:focus-visible` | client-ui + `.btn` / `.form-control` |
| Skip link | `Ir para o conteúdo` no layout operacional |
| Touch 44px | `--client-touch` + min-height botões |
| Labels | forms existentes |
| ESC / overlay | drawers/modais existentes (não alterados) |
| Disabled | opacity + cursor |
| Contraste | ThemeService (inalterado) |
| Ícones | `aria-hidden` nos decorativos; close com `aria-label` na Equipe |

Mapa: house pins + marks 8.2.29 continuam a pista para daltonismo.
