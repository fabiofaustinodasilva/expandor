# FEEDBACK

## Toasts

`.op-toast` no layout operacional: fixed, bottom (mobile) / canto (desktop), `.success` / `.error`, close implícito por timeout existente.

## Alerts

`.alert-success` / `.alert-error` + `x-client.alert`. Integração Google Maps passou de card com borda colorida para esses alerts.

## Commission Reward

Componente especial (`#commission-reward`, emoji 🪙). **Não substituído por toast.**

## Loading

- Botão: disabled + padrão existente
- Listas: `x-client.skeleton` / `x-client.loading` onde já havia
- Mapa: loading atual preservado
