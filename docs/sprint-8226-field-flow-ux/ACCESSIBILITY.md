# ACCESSIBILITY

- `role="dialog"` + `aria-modal="true"`
- `aria-labelledby` nos títulos
- Fechar com X / Cancelar / backdrop (existente)
- Hint vazio: `role="status"` + `aria-live="polite"`
- Touch targets ≥ ~44px nos CTAs
