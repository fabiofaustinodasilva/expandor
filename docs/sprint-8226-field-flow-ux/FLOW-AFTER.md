# FLOW-AFTER

```
Mapa → toque → NOVO PONTO (sheet)
     → marker → REGISTRAR VISITA (mesmo sheet)
     → Venda realizada → CONFIRMAR VENDA (mesmo sheet, bloco sale-finalize)
     → reward overlay + som (inalterado)
```

- Header + body scrollável + footer fixo
- Cancelar secundário / CTA primário
- Área vazia: hint 1×, não modal
- GPS opcional; toque no mapa suficiente
- Drawer passivo (`body.map-operation-open`)
