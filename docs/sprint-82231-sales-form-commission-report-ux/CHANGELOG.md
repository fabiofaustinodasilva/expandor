# Changelog

## Added

- Coluna Valor da venda em `/comissoes`
- Helpers `historicalSaleAmount()` / `historicalSaleAmountLabel()` em `SalesCommission`
- Eager load `saleItem` no repository

## Fixed

- `#visit-modal` / `#point-modal`: sheet com header/body/footer e scroll só no body

## Unchanged

- Cálculo de comissão, snapshots, reward UX, Google Maps
