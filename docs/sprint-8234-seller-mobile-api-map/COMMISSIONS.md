# Comissões

Seller vê somente as próprias (`user_id` forçado no service).

Lista + summary já existentes (`total_amount`, `sales_count`, pending/approved/paid). Sem métrica nova.

Reward overlay + `commission-coins.wav` local após `commission_awarded.play_reward` no 2xx da venda.
